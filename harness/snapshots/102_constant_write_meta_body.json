{
  "fixture": "harness/corpus/102_constant_write_meta_body.rb",
  "diagnostics": [
    {
      "rule": "call.unresolved-toplevel",
      "line": 68,
      "column": 19,
      "severity": "warning",
      "message": "unresolved toplevel call to `parent_of`. If a project file defines `parent_of` via a toplevel `def` or a monkey-patch on Object/Kernel, list that file in `.rigor.yml`'s `pre_eval:` (ADR-17) so the analyzer sees it."
    },
    {
      "rule": "call.unresolved-toplevel",
      "line": 95,
      "column": 1,
      "severity": "warning",
      "message": "unresolved toplevel call to `some_dsl_call`. If a project file defines `some_dsl_call` via a toplevel `def` or a monkey-patch on Object/Kernel, list that file in `.rigor.yml`'s `pre_eval:` (ADR-17) so the analyzer sees it."
    },
    {
      "rule": "call.unresolved-toplevel",
      "line": 96,
      "column": 3,
      "severity": "warning",
      "message": "unresolved toplevel call to `attr_reader`. If a project file defines `attr_reader` via a toplevel `def` or a monkey-patch on Object/Kernel, list that file in `.rigor.yml`'s `pre_eval:` (ADR-17) so the analyzer sees it."
    },
    {
      "rule": "call.unresolved-toplevel",
      "line": 110,
      "column": 3,
      "severity": "warning",
      "message": "unresolved toplevel call to `attr_reader`. If a project file defines `attr_reader` via a toplevel `def` or a monkey-patch on Object/Kernel, list that file in `.rigor.yml`'s `pre_eval:` (ADR-17) so the analyzer sees it."
    },
    {
      "rule": "call.unresolved-toplevel",
      "line": 115,
      "column": 3,
      "severity": "warning",
      "message": "unresolved toplevel call to `attr_reader`. If a project file defines `attr_reader` via a toplevel `def` or a monkey-patch on Object/Kernel, list that file in `.rigor.yml`'s `pre_eval:` (ADR-17) so the analyzer sees it."
    }
  ]
}
