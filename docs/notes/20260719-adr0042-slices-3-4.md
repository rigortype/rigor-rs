# ADR-0042 implementation — Slices 3–4 (2026-07-19)

The nested-class INSTANCE witnessing follow-up to Slices 1–2 (which closed the
singleton path). Both slices are narrow, project-sig-only changes: they alter
behavior ONLY on a nested-leaf collision, so configless corpora are untouched.

## Slice 3 — qualified instance witnessing (shadow-sig soundness fix, fixture 70)

The residual defect-2 UNSOUNDNESS: a toplevel project-sig `Status`/`Instance`
still had the nested-stdlib surface (`Process::Status#exited?`,
`Digest::Instance#digest`) short-key-MERGED into it and silently accepted.

- `qualified_class_has_method`: instance-method existence over the ISOLATED
  qualified entry (project `Status`'s own surface, NOT the merge). The leaf's
  own methods + instance aliases come from the qualified entry; ancestors
  (superclass/includes — top-level/global names) resolve through the existing
  short chain walk, with a class's implicit `Object` defaulted. Differs from
  `class_has_method` ONLY on a nested-leaf collision.
- the source-range project-sig witness now checks it, so a shadow class
  witnesses a stdlib-only member as absent — matching the oracle's namespace
  isolation.

Fixture 70 fully closed (4/4). Live 213→215 matched, 4→2 gaps, 0 FP; core
corpora 0 FP; survey source-class stress (dependabot 2, rails 67) == baseline.

## Slice 4 — nested project-sig `.new` typo (fixture 69)

`Outer::Inner.new.spni` — the reference witnesses through the QUALIFIED path;
rigor-rs was blind (`is_project_sig_class` is short-key and misses
`Outer::Inner`).

- `qualified_project_sig_classes` — the qualified twin of `project_sig_classes`
  (same before/after-sig snapshot) + `is_qualified_project_sig_class`.
- the witness gate fires for a toplevel known class OR a nested qualified
  project-sig class, both over the isolated qualified surface.
- the `.new` typing already worked: Slice 2's SourceIndex Pass-2 registers the
  qualified ConstantRead, so `Outer::Inner.new` mints the nominal.

Fixture 69 closed. Live 215→216 matched, 2→1 gaps, 0 FP; core + survey clean.

## Open (last gap)

Fixture 68's `Process::Status` INSTANCE via `Process.wait2` — needs the
tuple-return typing to carry `Process::Status`, then instance witnessing on
that qualified-class-typed value (not a `.new` mint). A separate slice; the
qualified instance substrate (`qualified_class_has_method`) is already in
place, so it is a value-typing wire-up, not new index machinery.
